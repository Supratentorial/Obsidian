///<reference path="interfaces/interfaces.ts"/>

module data.repository{

    export class MatterRepository implements data.interfaces.IRepository{
        create(object: data.models.Matter) {
        }

        remove(id:number) {
        }

        update(id:number) {
        }

        getById(id:number) {
        }

    }
}